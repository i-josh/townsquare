import 'package:flutter/material.dart';

class PostDetailMobile extends StatelessWidget {
  const PostDetailMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
